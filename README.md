# simstore

## Usage

**Compile**

```{Shell}
cd service && oasis build
```

**Start local chain**

```{Shell}
oasis chain --interface 0.0.0.0
```

**Start Demo**

```{Shell}
cd app && yarn serve
```
